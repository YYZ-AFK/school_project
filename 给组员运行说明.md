# 慧医数字医疗应用系统 - 组员运行说明

## 1. 先完整解压

收到项目后，先完整解压到普通文件夹里，不要在压缩包里直接双击运行。

## 2. 必装软件

```text
JDK 8 或更新版本
MySQL 8
Edge / Chrome 浏览器
```

Node.js 不是必装。装了 Node.js 会用 Node 启动前端；没装时脚本会自动使用 PowerShell 静态服务器。

## 3. 一键启动

双击：

```text
run.bat
```

脚本会自动检查 MySQL、检查 `bin_text` 数据库、缺少数据时导入数据库，然后启动：

```text
后端：http://127.0.0.1:8080
前端：http://127.0.0.1:5173
```

## 4. 登录

账号和密码请向项目负责人获取。不要把真实账号密码写在截图、说明文件或公开聊天里。

## 5. 常见问题

如果 `8080` 被占用，先关闭旧的后端窗口，或在任务管理器结束占用端口的 `java.exe`。

如果 `5173` 被占用，先访问 `http://127.0.0.1:5173` 看前端是否已经打开；如果不是本项目，关闭占用端口的程序。

如果数据库导入失败，确认 MySQL Server 已安装并正在运行。只安装 MySQL Workbench 不够，必须有 MySQL Server。

## 6. 关闭项目

删除、移动、重新压缩项目文件夹前，先双击：

```text
stop-all.bat
```
