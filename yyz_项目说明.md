# 慧医数字医疗应用系统 - 项目说明

## 项目简介

慧医数字医疗应用系统，包含 Spring Boot 后端、Vue 前端页面、MySQL 数据库脚本和一键启动脚本。

---

## 技术栈与环境要求

| 组件 | 版本/说明 |
|------|-----------|
| **后端** | Spring Boot（JDK 1.8） |
| **前端** | Vue（medical-frontend） |
| **数据库** | MySQL 8 |
| **浏览器** | Edge / Chrome |
| **Node.js** | 非必装。已装 Node.js 则用 Node 启动前端，未装时脚本自动使用 PowerShell 静态服务器 |

---

## 快速启动

收到项目后，**先完整解压到普通文件夹**，不要在压缩包里直接运行。

### 一键启动（推荐）

双击项目根目录下的：

```
run.bat
```

或：

```
start-all.bat
```

脚本会自动检查 MySQL、检查数据库、缺少数据时导入，然后启动前后端。

启动后本机访问：

```
http://127.0.0.1:5173
```

后端 API 地址：`http://127.0.0.1:8080`

---

## 网络访问

### 校园网 / 局域网访问

你和同学在同一个局域网时，先启动项目，再让同学打开启动窗口里显示的局域网地址，格式类似：

```
http://172.x.x.x:5173
```

如果同学打不开，双击 `allow-lan-access.bat`，出现管理员授权时点"是"，放行后再试。

### 公网访问（cloudflared 隧道）

适用于不在同一网络的人访问：

1. 确认本机可以打开 `http://127.0.0.1:5173`
2. 双击 `start-public-url.bat`
3. 等窗口出现 `PUBLIC_URL=https://xxxxx.trycloudflare.com`
4. 把该公网地址发给访问者

> **注意：**
> - 公网窗口不能关闭，关闭后地址会失效
> - 演示期间不要关闭任何启动窗口
> - 别人通过公网修改的数据会直接写入你的本地 MySQL
> - 如果开着 Clash Verge / Mihomo，请关闭虚拟网卡或 TUN 模式，否则可能出现 `198.18.x.x`、TLS handshake、Bad Gateway 等问题

---

## 数据库说明

### 数据库文件

数据库文件位于 `database/` 目录下：

| 文件 | 说明 |
|------|------|
| `bin_text2.sql` | **推荐使用** — 数据库结构文件 |
| `seed_more_demo_data.sql` | 演示数据填充脚本 |
| `bin_text.sql` | 旧版数据文件（数据量大，单条插入，不推荐） |

### 数据库配置

- **数据库账号密码**：存储在 `account` 表中
- **修改密码**：使用项目内的 `PasswordUtil` 工具类（后端 `medical-backend/src` 下）
- 脚本启动时自动检查 `bin_text` 数据库是否存在，不存在则自动导入
- 也可手动导入：双击 `import-database.bat`

---

## 登录账号

> 登录账号和密码请向**项目负责人**获取。
>
> 不要把真实账号密码写在截图、说明文件或公开聊天里。

---

## 脚本清单

| 脚本 | 用途 |
|------|------|
| `run.bat` | 一键启动前后端（推荐入口） |
| `start-all.bat` | 启动后端 + 前端 |
| `start-backend.bat` | 单独启动后端（端口 8080） |
| `start-frontend.bat` | 单独启动前端（端口 5173） |
| `import-database.bat` | 手动导入数据库 |
| `allow-lan-access.bat` | 添加防火墙规则，允许局域网访问 |
| `start-public-url.bat` | 创建 cloudflared 公网隧道 |
| `stop-all.bat` | 停止所有进程（删除/移动/压缩项目前先执行） |

---

## 常见问题

### 端口 8080 被占用
关闭旧的后端窗口，或在任务管理器里结束占用端口的 `java.exe`。

### 端口 5173 被占用
先访问 `http://127.0.0.1:5173` 看前端是否已经打开；如果不是本项目，关闭占用端口的程序。

### 数据库导入失败
确认 **MySQL Server**（不是 MySQL Workbench）已安装并正在运行。只有 MySQL Workbench 不够，必须有 MySQL Server。

### 公网地址打不开
检查是否开着代理软件（Clash Verge / Mihomo / VPN），关闭 TUN 模式或虚拟网卡后重试。

---

## 关闭项目

在删除、移动、重新压缩项目文件夹前，务必先双击：

```
stop-all.bat
```

它会停止占用 `8080` 和 `5173` 的后端、前端进程。
